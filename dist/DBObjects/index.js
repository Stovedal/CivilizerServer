'use strict';

var users = require('./objects/users');
var scores = require('./objects/scores');
var civilizations = require('./objects/civilizations');

module.exports = {
  users: users,
  scores: scores,
  civilizations: civilizations
};